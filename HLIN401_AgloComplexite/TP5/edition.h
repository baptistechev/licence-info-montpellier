#include <iostream>
#include <string>

#include "matrice.h"

int valeur(int**, int, int);
int min(int, int, int);
int distanceEdition(string, string, int**);
string alignement(string&, string&, int**);
